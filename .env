# Use relative base so Vite dev server proxy can forward /api to your backend
VITE_API_URL=/api
VITE_API_BASE_URL=/api

# Backend host the Vite proxy should forward to (override if backend is on another host)
VITE_API_SERVER=http://127.0.0.1:8000

# Set to 1 to disable the Vite /api proxy (useful while backend is offline)
VITE_DISABLE_PROXY=0
